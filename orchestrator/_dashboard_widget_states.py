# Copyright 2026 Geser Dugarov
# SPDX-License-Identifier: Apache-2.0
"""Historical import site for the page's two dead ends and its footer.

The startup state a database with nothing in it is answered with, the notice a
window matching no row renders, and the line the page closes on are the
dashboard owner's own objects, published here under the spellings the page
always imported them by -- the two messages public, the three renders under a
leading underscore. A caller that names this module gets those rather than
copies, so the banner an operator reads and the reads the page counted behind
it cannot come apart.
"""
from __future__ import annotations

from orchestrator.observability.dashboard import page_states


NO_DATA_MESSAGE = page_states.NO_DATA_MESSAGE
EMPTY_WINDOW_MESSAGE = page_states.EMPTY_WINDOW_MESSAGE
_render_dashboard_footer = page_states.render_dashboard_footer
_render_no_data = page_states.render_no_data
_render_empty_window = page_states.render_empty_window
