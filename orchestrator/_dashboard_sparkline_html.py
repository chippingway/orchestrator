# Copyright 2026 Geser Dugarov
# SPDX-License-Identifier: Apache-2.0
"""Historical import site for the inline SVG a sparkline is drawn as.

The default box a tile draws one in, the request a caller's line is described
by, the two path strings it is written as, the point rounding both share, the
renderer, and the historical keyword surface in front of it are the dashboard
owner's own objects. A caller that names this module -- or the HTML surface
above it -- gets those rather than a copy, so the strip a page renders and the
markup the owner writes cannot come apart.
"""
from __future__ import annotations

from orchestrator.observability.dashboard import sparkline_html


DEFAULT_SPARKLINE_WIDTH = sparkline_html.DEFAULT_SPARKLINE_WIDTH
DEFAULT_SPARKLINE_HEIGHT = sparkline_html.DEFAULT_SPARKLINE_HEIGHT
_SparklineRequest = sparkline_html.SparklineRequest
_sparkline_paths = sparkline_html.sparkline_paths
_sparkline_point_text = sparkline_html.sparkline_point_text
_sparkline_area_path = sparkline_html.sparkline_area_path
_render_sparkline = sparkline_html.render_sparkline
_sparkline_svg = sparkline_html.sparkline_svg
_SPARKLINE_SIGNATURE = sparkline_html.SPARKLINE_SIGNATURE
