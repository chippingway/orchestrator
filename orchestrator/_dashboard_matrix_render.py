# Copyright 2026 Geser Dugarov
# SPDX-License-Identifier: Apache-2.0
"""Historical import site for the invocation-level skill matrix.

The notice a window with no catalog-backed cells renders instead, the rules the
panel scopes to its own class, and the sorted table itself are the dashboard
owner's own objects. A caller that names this module gets those rather than a
copy, so the matrix a page draws and the one the owner builds cannot start
answering differently about what a repository's runs reached for.
"""
from __future__ import annotations

from orchestrator.observability.dashboard import skill_matrix


SKILL_MATRIX_EMPTY_MESSAGE = skill_matrix.SKILL_MATRIX_EMPTY_MESSAGE
SKILL_MATRIX_EXTRA_CSS = skill_matrix.SKILL_MATRIX_EXTRA_CSS
_skill_matrix_html = skill_matrix.skill_matrix_html
